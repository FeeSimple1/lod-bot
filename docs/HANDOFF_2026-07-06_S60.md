# Handoff — July 6 2026, after Session 60

HEAD: see git log (3 commits this session).  All green: 1,414 + 101
tests, gates 1-20, soak 120 DONE clean, balance rebaselined, fresh
300-game baseline (docs/balance_largeN_s60_q22.md).
Opener: "Read docs/HANDOFF_2026-07-06_S60.md and continue from Next."

## The session in one paragraph
Example 2 golden landed (one real fix: target-aware B2 bullet 3 —
card 42's Tory can only reach Connecticut; new tories_in field; the
Garrison body validated against the book with two asserts documented
as draw-dependent-legal).  Then Eric ruled Q22 = (b) and the RANDOM
SPACES TABLE went engine-wide: all 35 seeded-uniform tie sites across
the four bots now route through pick_by_priority /
choose_random_space (two-candidate D6 convention included).  The
fresh baseline measures the ruling itself: British 20/300 under
uniform ties -> 9/300 under the table — the tie distribution was
worth ~11 British wins, so the S59 "6-7%" was partly a uniform-ties
artifact.  S53-S59 large-N numbers are non-comparable from 9c1aa51.

## Next queue
1. Example 4 golden (Brilliant Stroke exemplar, Playbook 1325-1426)
   — the last untranscribed walk-through; S50's BS work has never
   been checked against it.
2. Tighten Examples 2/3 to exact-pick asserts now that table parity
   holds (the walk-throughs' narrated D3/D6 rolls should reproduce
   Connecticut/Boston/the Garrison draws exactly — extend
   _ScriptedRng scripting to the table rolls and assert the book's
   picks; any residual mismatch is a real walk-semantics bug).
3. Indian 1775 concentration (43/300 under both tie regimes): the
   Royalist-side S59/S60 fixes moved the Indians, not the table.
   Audit-first: their §8.7 rows are verified and the Example-5 golden
   is green, so next instruments are goldens 2-4 side effects and a
   margin-source read (which condition wins those games — villages
   (cond2) or sup-opp (cond1)?).
4. Piece 3 card ledger — fold in the S60 class note: every
   fixed-target places_tories card needs tories_in populated, and
   places_british_regulars is trivially true (target-aware treatment
   needed) — the card-42 pattern likely repeats across the deck.
5. TRACEABILITY_CH1_7 C-series; Pieces 4/6/9.

## Method additions (S60)
- Non-player ties: use pick_by_priority(state, [(key, sid), ...])
  with SUBSTANTIVE keys only — never embed rng in sort keys.  Payload
  that rides along with candidates (adj lists, needed counts) gets
  re-attached via dicts after ordering (see the Indian supply/march
  sites for the pattern).
- Heredoc gotcha: large triple-quoted replacement literals containing
  nested parens/brackets have twice broken `python3 - <<'PYEOF'`
  scripts mid-file — build multi-line replacements from
  "\n".join([...]) line lists instead (two S60 scripts died to this;
  the second write never happened and the file was left on the
  PREVIOUS script's state — always re-grep after a failed script).
- The engine sandboxes bot turns via deepcopy: any rng wrapper needs
  __deepcopy__ and must not forward dunders (S59 note; bit again).

## Awaiting Eric
Nothing blocking.  Q1-Q22 all resolved.  Standing meta-question now
sharper: with all flowcharts text-verified, four goldens green, and
the RULED tie procedure in place, the British sit at 3% and the
Indians at 22% overall.  The remaining fidelity instruments are
Example 4 + exact-pick tightening + the card ledger; after those,
faction strength is a design question, not a bug hunt.
