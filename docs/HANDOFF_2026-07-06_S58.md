# Handoff — July 6 2026, after Session 58

HEAD: see git log (3 commits this session).  All green: 1,411 + 101
tests + 1 strict-xfail golden (deliberate — see below); no behavioral
change landed, so no gate/soak/rebaseline was owed this session.
Opener: "Read docs/HANDOFF_2026-07-06_S58.md and continue from Next."

## The session in one paragraph
Eric ruled Q20 = (b) just-enough French March and Q21 = (a) Spread
with an implicit not-already-blockaded qualifier — both match the
implementation, so QUESTIONS.md is now Q1-Q21 ALL RESOLVED and the
four code comments that cited interim behavior now cite the rulings.
Piece 5 then delivered: the Example-5 Playbook golden (Indian Scout +
War Path) exposed THREE deviations the flowchart-level I-node sweep
could not see — (D1) the Scout moves NO War Party and no Tory
(§3.4.3 legality: at least one WP must move and Activate; §8.7.4
"Move one War Party and the most Regulars and Tories possible";
§8.1.2 alternation R,T,R yields the Playbook's 2R+1T), (D2) the
Brant leader-follow ignores the moving-equals-staying D6 (1-3
follows) and teleports toward an uninvolved space, (D3) War Path
ignores the Village-space tiebreak and removes a Continental where
§8.1.2 takes Militia first.  The Scout's embedded Skirmish, fees,
and CBC/CRC accounting match the Playbook exactly.  All three are
ROYALIST-strength fixes — the first British-side fidelity finds
since S55.

## Next queue
1. Fix D1/D2/D3 (the xfail in test_playbook_goldens.py carries the
   full citation trail; strict=True, so the fixture flips loudly to
   XPASS when correct — remove the mark then).  Scout body:
   indians.py _scout_sequence + commands/scout.py (composition +
   Activation); leader-follow: the Indian OPS follow layer (grep
   "follows largest WP group"); War Path: indians.py war_path space
   selection + piece choice.  FULL battery + 300-game large-N after
   (these change Indian and British trajectories).
2. Playbook goldens, Examples 2-4 (line ranges in the fixture
   header): Example 2 Garrison+expulsion, Example 3 Muster+Skirmish,
   Example 4 Brilliant Stroke (§8.3.7 exemplar).  Use the Example-5
   pattern: assert printed-setup PRECONDITIONS first, script the
   narrated dice via _ScriptedRng, isolate the acting faction with
   state["ineligible_next"] (play_card's _prepare_card resets the
   eligible map, so writing state["eligible"] directly does NOT
   isolate — this cost a probe cycle to discover).
3. Then the remaining Piece 3 card ledger, TRACEABILITY_CH1_7
   C-series, Pieces 4/6/9.

## Method (unchanged; two additions)
S56/S57 handoff method holds.  Additions: (1) goldens assert
preconditions from the printed setup before acting, and use
strict=True xfail for documented divergences so fixes self-announce;
(2) flowchart-level sweeps do NOT retire body-level risk — the
goldens are the instrument for that layer (Example 5 proved it one
session after a clean I-node sweep).

## Awaiting Eric
Nothing.  Q1-Q21 all resolved.  The standing game-meta question
(is bot-vs-bot British ~1% the design?) now waits on the D1-D3
fixes + remaining goldens, which are Royalist-side strength
corrections and may move the number on their own.
