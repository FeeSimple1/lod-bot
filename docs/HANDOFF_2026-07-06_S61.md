# Handoff — July 6 2026, after Session 61

HEAD: see git log (1 commit this session, on top of the S60 pair).
All green: 1,415 + 101 tests, gates 1-20, soak 120, rebaselined,
largeN_s61.jsonl fresh.  All FIVE Playbook goldens green.
Opener: "Read docs/HANDOFF_2026-07-06_S61.md and continue from Next."

## The session in one paragraph
Example 4 (British Brilliant Stroke) is transcribed and green, and it
rebuilt the §8.3.7 machinery: the LimCom selection now rolls the B6
die and uses the S55 battle gate; both LimComs execute through the
bots' own planners (limited+free, Leader tied to LimCom1 via
state["_bs_leader_origin"]); the second LimCom is flowchart-selected.
Two deep non-BS bugs fell out: HOWE'S §1.9 ability had lowered FNI
while LEAVING the Blockade marker on the City (a phantom blockade
zeroing Support/income forever — project-lifetime), and the
limited-Muster Tory cap blocked Tories from the Muster's own single
space.  British now 23/300 with a scenario-shaped 21% in 1776 (Howe
is the 1776 starting leader).  Indians deepen to 54% in 1775.

## Next queue
1. INDIAN 1775 OUTLIER (54%, rising with every Royalist fix): margin-
   source read first — which condition wins those games (villages
   cond2 vs sup-opp cond1)?  diag_s55.py pattern; add village/fort
   counts.  All §8.7 rows are verified and the Example-5 golden is
   green, so if the read shows legal dominance, it goes to Eric as
   design-meta; check the §8.7.2 Gather gate interaction with the
   S61 Howe/support changes first.
2. Exact-pick tightening of Examples 2/3 (Q22 table parity): script
   the table D3/D6 rolls and assert Connecticut/Boston/the Garrison
   draws; residual mismatches are real walk-semantics bugs.  NOTE
   from Example 4 debugging: destination candidate SETS shift with
   support/blockade state, changing whether the 2-candidate D6 or the
   full walk consumes dice — assert rng_log labels alongside picks.
3. Piece 3 card ledger + the tories_in/places_british_regulars
   target-aware sweep (S60 class note).
4. Possible Q23: the Playbook's ToA "French Preparations = 16" vs
   the S50 Available-only formula (§8.1 bullet) — matters only if a
   future golden needs the ToA trigger itself.
5. P/F/I leader-tie for BS LimCom1 (the _bs_leader_origin hook is
   British-only; the other bots' planners ignore it — their BS
   LimComs run unrestricted, a documented §8.3.7 gap).

## Method (S61 additions)
Everything from S60 holds.  New: BS work MUST route through
_run_bot_limcom (never dispatcher-with-space); when a golden's die
script misbehaves, print rng_log FIRST — the consumption order
depends on candidate-set sizes (2-candidate D6 vs full walk), which
depend on board state earlier in the same turn.

## Awaiting Eric
Nothing blocking.  Q1-Q22 resolved.  The sharpest open picture yet:
Royalist 37% overall, British 21% in the Howe scenario, Indians 54%
in 1775 — the fidelity wells are nearly dry (outlier read + exact
picks + card ledger), and the design-meta question is close to
answerable with real data.
