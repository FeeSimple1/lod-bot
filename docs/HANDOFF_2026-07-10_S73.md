# Liberty or Death Bot — Handoff Briefing (after Session 73) — for the next pass

Repo: https://github.com/FeeSimple1/lod-bot (branch `main`)
HEAD: see `git log` — all pushed, all green.

Suggested opener for a fresh session:
"Read `docs/HANDOFF_2026-07-10_S73.md` in the lod-bot repo, then
immediately start the next task — do not wait for confirmation."

## Access (Eric-approved)

Clone and push with Eric's fine-grained PAT (provided with the
briefing; not stored in-repo).  Standing authority to commit, push,
pull, and merge without asking.

**Cloud-environment git note (S73):** the managed sandbox routes
github.com through a credential proxy that 403s repos outside the
session's source set.  Clone works; for pushes use
`git -c http.proxy= push origin main` (direct egress is permitted and
uses the PAT embedded in the remote URL).

## Standing instruction (Eric — keep following it)

Roll continuously: when a step or milestone finishes, pick the next
natural step and start it.  Stop only for genuine human-adjudication
blockers.  Log rules questions to `QUESTIONS.md` and keep moving.
Batch progress reports at natural milestones.

## >>> START HERE <<<

1. **Piece 3 remaining backlog** (ROADMAP has the list; S73 closed the
   §8.3.5 residuals).  In rough order of value:
   * `event_eval.py` flag audit against all four flowcharts' actual
     "Event or Command?" bullets — only B2 has had a dedicated session;
     the S73 question-spaces work touched B2/P2 bullets and confirmed
     F2/I2 have no space-conditioned questions, but the CAPABILITY
     FLAGS per card have never been swept against the sheets for
     P2/F2/I2.
   * `event_instructions.py`: transcribe the full per-faction Special
     Instructions (flowchart reference txt files in `Reference
     Documents/`) and diff against the pared-down PATRIOTS / INDIANS /
     FRENCH dicts.
   * Dict-order FIRST-FIT fallback scans in the card handlers: ~25
     `for sid in state["spaces"]` sites remain across the three war
     files (S73 count).  The S68 Q22 sweep converted the
     rng-in-sort-key class; these are a DIFFERENT class — override-
     fallback scans that take the first qualifying space in dict order
     (e.g. cards 47/55/76/77 defaults).  Each needs a card-text read:
     if the scan encodes a sheet priority it stays (add the citation);
     if it picks among equal candidates it goes through
     pick_by_priority/pick_random_spaces (§8.2).  This subsumes the
     ROADMAP's stale line-number list.
   * `auto_place_blockade` stub: rules-check, implement or delete.
   * Cross-check audit_report.md session lists against all 109 cards
     for never-audited cards.
2. **Piece 7 residual (logged, medium value):** free operations
   GRANTED to a human faction by an event (e.g. card 15's Patriot
   March/Battle/Partisans) are still bot-planned in `_drain_free_ops`
   rather than wizarded for the human seat.  The CLI event-choice
   layer (`lod_ai/event_choices.py`) is DONE for all 43 choice-bearing
   cards — this is the one human-mode gap left.

## TL;DR (state at S73)

Everything green: ~1,530 tests across both roots, clean-sweep gate
seeds 1-20 with invariants on all three scenarios, 120-game invariant
soaks clean, playbook goldens at exact picks, human_qa green across
all seatings, balance canary rebaselined after each behavioral change.
**GitHub Actions CI verified GREEN end-to-end at HEAD** (all five
jobs).  Note: the gradual-mypy job had been RED since S69 — implicit-
Optional parameters in commands/battle.py reached through
util/year_end.py's import — masked because prior handoffs' "all green"
covered the local battery only.  Fixed in S73 (typing-only).  Check
Actions, not just the battery, before claiming green.
Q1-Q22 RESOLVED; **Q23 OPEN awaiting Eric** (Propaganda/Raid marker
stacking; interim = conserving skip semantics); Q15 blocked on missing
source materials.  **The tune-or-not judgment call is Eric's and
non-blocking** — see the standing balance picture below.

## Session 73 in one list (all pushed)

* **Large-N instrument refresh ×3** (`docs/balance_largeN_s72.md` +
  two addenda): 300 games each, seeds 5000+, PYTHONHASHSEED=0.  All
  three reads inside the same CIs — the standing picture for Eric's
  tune call: **French ~45-47%, Indians ~22-26%, Patriots ~19-21%,
  British ~10%; Royalist ~33-36%.**  No fidelity leads remain on the
  numbers.
* **Piece 7 COMPLETE** (3 commits): `lod_ai/event_choices.py` — the
  CLI event-choice collection layer for all 43 choice-bearing cards
  (26 space, 6 sub-option, 11 faction/mix), driven by per-card Step
  specs whose candidate menus mirror each handler's own legality
  filters.  Named-faction deciders prompt only when that seat is
  human; handlers re-validate everything.  Overrides pop after
  handle_event.  Audit-table correction: card 29's value is a FACTION.
  Gates: tests/test_event_choices.py + the frozen completeness
  registry.  human_qa fuzzes the prompts (ScriptedProvider answers
  every menu).
* **Piece 3 §8.3.5 residuals CLOSED** (2 commits):
  - Per-card who-choice audit across all 109 cards; fixed onto
    util/target_order: card 74 (recipient hardwired INDIANS), card 48
    (dict-order faction pick), card 67 (Royalist executor hardwired
    FRENCH), card 87 (executor-blind removal order), card 29
    (identical outcomes, now rule-cited).  beneficiary_order hardened
    against unknown executors.
  - Q22 residual: 15 pre-Q22 tie sites still alive in the four bot
    files (rng-in-sort-key + dict-order) — card-80 presets, British
    garrison phase-2b, displacement city+province, all four leader
    redeploys, Cornplanter, loyalist desertion — all converted to the
    §8.2 table (pick_by_priority / pick_random_spaces).  ZERO
    rng-in-sort-key sites remain engine-wide.  One test rewritten (its
    "different-pop" provinces were both Pop 2 — passed on seed luck).
  - §8.3.5 flowchart-question-spaces-first: space-conditioned B2/P2
    bullets record their matching set in state["_event_q_spaces"];
    pick_by_priority ranks those spaces above ALL substantive keys,
    pick_random_spaces exhausts them first; base_bot clears the key
    after the handler (free ops keep faction priorities).  F2/I2 have
    no space-conditioned questions.  test_event_question_spaces.py.

## Next queue (after START HERE)

1. Q23 ruling (Eric) → implement if "counts" model wins.
2. Tune decision (Eric) → new work stream with the 300-game Wilson
   instrument (largeN_s73b.jsonl is the current baseline read).
3. T6 residual: the Indian Raid node's mid-command "Plunder then Trade
   before completing" (I-flowchart) is unverified.
4. C6 §1.4.1 (voluntary take-own-forces-from-map when type is not
   Available) — no reachable trigger; implement only if one appears.
5. Piece 8 statistical validation at scale (1,000 games/scenario) if
   Eric wants deeper instrumentation.

## Key documents

This file · `ROADMAP.md` (Piece 3 partial, 7 done, 9 done) ·
`docs/human_mode_audit.md` (wiring status; Piece 7 residual) ·
`QUESTIONS.md` (Q23 OPEN) · `TRACEABILITY.md` (T7 residuals closed;
§8.3.5 row updated) · `lod_ai/event_choices.py` ·
`lod_ai/tests/test_event_question_spaces.py` ·
`docs/balance_largeN_s72.md` (+S73 addenda — CURRENT baseline).

## Working method (established — follow it)

1. Rules first, cite everything.  New reference conflicts →
   `QUESTIONS.md`, STOP that item, keep working elsewhere.
2. Battery per behavioral change: chunk `lod_ai/tests`
   (`--ignore=lod_ai/tests/commands`), then `commands`+`tests`; gate
   `--seeds 1-10` then `11-20`; soak 120 with `--invariants` in ~36s
   slices until DONE (delete the old jsonl first); `balance_smoke
   --update` on winner flips; goldens must hold at exact picks.
3. Environment gotchas (S73-verified for the CLOUD sandbox):
   * Read/Edit file tools SHARE the filesystem with bash here — use
     them; the old "different filesystem" gotcha applied to earlier
     local-VM sessions only.  Heredoc literal-replacement still best
     for docs with tricky quoting.
   * `pip install pytest mypy --break-system-packages`; pytest with
     `-p no:cacheprovider`; PYTHONHASHSEED=0 for gate/balance/soak.
   * Keep bash calls ≤ ~40s (soak/large-N in ~37s slices).
   * `git -c http.proxy= push` (see Access above).
   * Non-player SPACE ties: pick_by_priority with SUBSTANTIVE keys
     only (never rng in a sort key — Q22; the lint + a clean grep now
     enforce this).  FACTION ties: util/target_order or explicit
     seeded roll.
   * Subagent audits work for read-only sweeps BUT verify every
     finding yourself (the S73 sweep agent was accurate, but one of
     its two "non-conforming" calls needed a rules re-read to confirm).
