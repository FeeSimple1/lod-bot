# Handoff — July 6 2026, after Session 57

HEAD: see git log (2 commits this session + the S56 tail).  All
green: 1,411 + 101 tests, gates/soak/balance per the S56 clusters
(no behavioral change landed in S57 itself — it was verification).
Opener: "Read docs/HANDOFF_2026-07-06_S57.md and continue from Next."

## The session in one paragraph
The §8.7 I-node sweep came back CLEAN (two parallel audits +
personal spot-checks of the I3 Support+1D6 dispatcher gate, the
§8.7.9 all-but-1-UG-WP defender hook, and Q18 replenish conformance)
— the S48/S51 Indian work had already fixed the bodies.  With that,
the FOUR-FACTION FLOWCHART INVENTORY IS COMPLETE: every §8.4-§8.7
row in TRACEABILITY.md is text-verified.  Piece 5 then began paying
out immediately: the Playbook's five "Non-Player Examples of Play"
are designer-authored ground truth, and Example 1 (British Event,
card #28, 1776) transcribed into test_playbook_goldens.py passes ON
FIRST RUN — the engine reproduces the designer's walk-through
exactly.

## The live problem (unchanged this session)
British ~1% (largeN_s56b.jsonl: Rebellion 269/300).  Q20/Q21 stay
open with Eric (restated in full in the S57 chat; Q21's two readings
are measured at 94% vs 86% Rebellion — an Indians 2x swing, British
flat either way).  With all flowcharts verified, the remaining
fidelity instruments are the four untranscribed Playbook examples —
especially Example 2 (Garrison movements + expulsion) and Example 3
(Muster+Skirmish), which exercise exactly the commands the British
bot lives on, and Example 4 (the §8.3.7 Brilliant Stroke exemplar,
which S50 fixed blind).  If those replay clean too, the fidelity
case is closed and the ~1% question goes to Eric as game-meta.

## Next queue
1. Playbook goldens, Examples 2-5 (line ranges in the
   test_playbook_goldens.py header; each needs its "alterations
   noted below" transcribed from the Playbook text into the fixture
   setup).  Assert the documented piece movements and SA choices.
2. When Eric rules Q20/Q21: implement + full battery + large-N
   rebaseline (both questions' interim behavior is cited in code
   comments referencing the Q numbers).
3. Remaining Piece 3 card ledger; TRACEABILITY_CH1_7 C-series
   (C3-C6, C8-C10); Pieces 4/6/9.
4. Standing meta-question for Eric (sharpened): all four factions'
   flowcharts text-verified + first golden green -> is bot-vs-bot
   British ~1% the physical game?  The Playbook's own strategy notes
   (p.~838-850) describe the British as needing Indian Raids to
   check Opposition — the design may simply expect Royalist
   coordination that two independent bots don't produce.

## Method (unchanged)
Everything from the S56 handoff holds (battery discipline,
PYTHONHASHSEED=0, ~/work clone, heredoc edits with count asserts,
40s bash budget, 300-game Wilson instrument, verify-agent-findings).
One addition: for goldens, assert PRECONDITIONS from the printed
scenario setup first (the Example-1 fixture checks MA's setup pieces
before playing the card) so a future setup change fails loudly as a
setup regression rather than a false golden failure.

## Awaiting Eric (all non-blocking)
Q20 (French March commitment), Q21 (Blockade stacking — measured),
and the game-meta judgment above.
