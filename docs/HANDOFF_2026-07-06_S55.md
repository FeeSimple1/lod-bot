# Handoff — July 6 2026, after Session 55 (single-session stretch)

HEAD: see git log (4 commits this session).  All green: 1,403 +
101 tests both roots, gate seeds 1-20 invariants-on, 120-game soak,
balance rebaselined per cluster, two 300-game large-N runs.
Opener: "Read docs/HANDOFF_2026-07-06_S55.md and continue from Next."

## The session in one paragraph
Queue item 1 (§8.4 part 2, the British 0-2% investigation) is DONE
end-to-end.  Headline: §8.1 "Paying Resource Costs" was unimplemented
— the Muster/March planners aborted the whole Command when the
4-space plan exceeded the purse, so the British passed 4-7 turns/game
holding Resources; now zero such passes.  Also: B9 gate per §8.4.3's
full comparison; §8.4.1 Skirmish now hunts CUBES (Glossary: Militia
are not cubes — the shared executor's removal order was backwards);
pre-battle Skirmish exclusion; Garrison §8.1 2-Resource gate + WI
count fix; full B-node inventory (Garrison: zero deviations); CC ctx
now reaches battle.execute (T13 selection/resolution mismatch — every
CC battle had resolved without its CC force); Village-only Indian
Reserve defense mod; march-in-place Tory stacks; Q19 filed AND
resolved by reference (Playbook p.~850: CC WPs absorb losses "as a
Tory" — implemented in the §3.6.7 alternation).

## The live problem (updated)
British now 1% / 5% / 2% (largeN_s55b.jsonl, n=300, seeds 5000+).
S54 was 0/1/2 — real movement in 1776, still far from parity.  BOTH
margins remain negative on average (sup-opp ≈ -3.6..-7.5, crc-cbc ≈
-3.0..-5.6; final scoring nets them, and the French margin is the
exact mirror — hence French 30-60%).  Casualty ledger (10x1775):
CBC = 12.9 Regulars + 6 Tories + 1 Fort/game; CRC = 8.7 French
Regulars + 4.2 Continentals.  Patriots battle 5-7x/game, British ~1
(B4/B6 route them to Garrison/Muster — per flowchart, text-verified).
Every §8.4.x row and B-node is now text-verified (TRACEABILITY.md
updated) — the "unimplemented British rules" well is EMPTY.  Next
suspects are on the OTHER side of the board: the F-nodes and
remaining P-node details have never had the equivalent inventory,
and French dominance may be THEIR missing restraints.  Diagnostic
instrument: diag_s55.py (per-game finals + command mix + pass
reasons; regenerate, not committed to jsonl).

## Next queue
1. F-node flowchart inventory (french.py vs "french bot flowchart
   and reference.txt" + §8.6) — the French win 30-60% everywhere and
   have never had a text pass; same method as today's B-node sweep.
   Note french.py contains U+202F chars.  Then remaining P-nodes
   (§8.5 beyond the S45 block) and I-nodes (§8.7 beyond S51).
2. Piece 5 Playbook goldens ("Reference Documents/Playbook
   Aug2016.txt") — it settled Q19 in one grep; transcribe the worked
   example(s) into replay fixtures.  High leverage on battle/CC/
   Brilliant-Stroke fidelity.
3. Remaining Piece 3: card-by-card ledger (cross-check audit_report
   session lists vs all 96 handlers; fold in the S49 card-73 note),
   T7/T8 general layers.
4. TRACEABILITY_CH1_7 C-series (C3-C6, C8-C10) + both matrices'
   verification queues; then Pieces 4/6/9.

## Method (unchanged — follow it)
Rules first, cite everything; QUESTIONS.md + stop only that item on
NEW conflicts (Q1-Q19 all resolved; Q19's Playbook citation is
flagged for Eric's confirmation, non-blocking).  Battery per
behavioral change (both roots incl. lod_ai/tests/commands/, gate
1-10/11-20, soak 120 — DELETE old soak.jsonl first, resumable
--max-seconds ~35; balance_smoke --update on winner flips is
per-change hygiene, NOT a strength reading).  Work in ~/work clone,
python3-heredoc edits with count asserts, re-verify imports per
batch, PYTHONHASHSEED=0 for all replays, bash calls <=40s (the
sandbox drops longer ones; on "already running" wait ~20s and retry).
The instrument is soak --games 300 --seed-base 5000 --out <f>.jsonl
+ Wilson (see docs/balance_largeN_s53.md); 20-game reads are noise.
Subagent audits work well for read-only rule-vs-code sweeps BUT
verify their findings before editing — one of three March "findings"
was a false positive (CC-to-Colony is correct: WPs may not enter
Cities, §4.2.1).

## Awaiting Eric
Nothing blocking.  Two standing judgment calls: (1) whether ~1-5%
non-player British could be true to the physical game's bot meta
(now better-informed: every British rule is text-verified, so any
remaining gap is either Rebellion-side bugs or the game itself);
(2) confirm the Q19 Playbook reading (QUESTIONS.md).
