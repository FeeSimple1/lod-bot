# Handoff — July 6 2026, after Session 62

HEAD: see git log (2 commits this session).  All green: 1,415 + 101
tests, gates 1-20, soak 120 DONE, rebaselined, largeN_s62.jsonl.
FIVE goldens green, Example 3 at EXACT PICKS.
Opener: "Read docs/HANDOFF_2026-07-06_S62.md and continue from Next."

## The session in one paragraph
The Indian 1775 outlier is DIAGNOSED, not fixed, because it is not a
bug: the wins are §7.3 final-scoring wins on the now-Royalist support
race (margins data in audit_report S62) — design-meta for Eric.
Example 3 is tightened to exact picks under Q22 table parity: the
book's dice reproduce the printed Muster to the piece (Connecticut
6R+2T then the Fort's 2R+1T exchange, NYC/PA pairs, Boston's single
Passive-Opposition Tory, Resources 5->1) after one more
planned-aware fix — the Tory adjacency check now sees the Regulars
this same Muster places in a NEIGHBOR (Boston-via-Connecticut).

## Next queue
1. Piece 3 card ledger + the target-aware event sweep (tories_in
   for every fixed-target card; places_british_regulars is trivially
   true today — the card-42 class).
2. P/F/I leader-tie for BS LimCom1 (_bs_leader_origin is
   British-only; §8.3.7 gap for the other three bots' Brilliant
   Strokes).
3. TRACEABILITY_CH1_7 C-series (C3-C6, C8-C10); Pieces 4/6/9;
   possible Q23 (Playbook ToA "Preparations = 16" vs the S50
   Available-only formula) if a golden ever needs the ToA trigger.
4. Standing for Eric: the design-meta question, now with its
   sharpest data — Royalist 34%, British 22% in the Howe scenario,
   Indians 45% in 1775 via final scoring.  Every flowchart row,
   five designer walk-throughs, and the ruled tie procedure are in;
   the remaining queue is breadth (card ledger), not depth.

## Method
S60/S61 additions hold.  New: when tightening goldens to exact
picks, pad the dice scripts — table consumers appear/disappear as
candidate-set sizes cross the 1/2/3 thresholds, and consumption
order follows the planning phases (assert against rng_log labels
when in doubt).

## Awaiting Eric
Nothing blocking.  Q1-Q22 resolved; the design-meta read above.
