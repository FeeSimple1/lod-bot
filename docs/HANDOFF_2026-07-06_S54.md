# Handoff — July 6 2026, after Session 54 (Sessions 45-54 stretch)

HEAD: see git log. All green: ~1,475 tests both roots, gate seeds 1-20
invariants-on, 120-game soaks, balance smoke rebaselined per change.
Opener: "Read docs/HANDOFF_2026-07-06_S54.md and continue from Next."

## The stretch in one paragraph
Patriot block (S45), Ch 1-7 traceability + §1.9 pop-0 (S46,
TRACEABILITY_CH1_7.md C-series), T14 sweep incl. card-80 self-target
(S47), T9 space picks + pick_cities data bug + Raid batch fix (S48),
event_eval audit incl. B2 dead Unavailable key + FNI flags (S49), T10:
**bot ToA was never declared — French were 0% in 1775/76 forever**
(S50), supply residuals + Indian nodes + card-15 §5.1.1 exemption
(S51), human-mode QA 4 games clean (S52, human_qa_run_s52.py), C7
NP-first ties + Piece 8 first large-N (S53,
docs/balance_largeN_s53.md), §8.4 part 1: Muster SA order
(Skirmish-first per 8.4.2 — S31 had Garrison's order) + Loyalist
Desertion re-scoring (S54).

## The live problem
**British win 0-2% in ALL scenarios** (300-game runs, S53+S54, CIs
exclude parity; largeN_s54.jsonl).  Muster/Desertion fixes did NOT
move it.  Next suspects in order: §8.4.4 Battle details vs text
(stale since S19-20), B-node flowchart inventory (never done), the
B2/B6/B9 decision-gate thresholds, CRC>CBC starvation analysis (CBC/
CRC trajectories are in the jsonl).  Do the audit BEFORE any tuning
— every "weak faction" so far was unimplemented rules.

## Next queue
1. §8.4 part 2: Battle text pass + B-node inventory + CBC/CRC
   trajectory read (the British 0-2% investigation).
2. Remaining Piece 3: card-by-card ledger (cross-check audit_report
   sessions vs all 96 handlers), T7/T8 layers.
3. Piece 5 Playbook goldens (untapped oracle; text in Reference
   Documents), then Pieces 4/6/9.
4. TRACEABILITY_CH1_7.md C-series (C3-C6, C8-C10) + verification
   queues in both matrices.

## Method (unchanged — follow it)
Rules first, cite everything; QUESTIONS.md + STOP on new conflicts
(Q1-Q18 are ALL RESOLVED — Q16 manual-over-flowchart and Q17
specific-flowchart were ruled and implemented in Session 32; rulings
stay case-by-case, do not generalize); battery per behavioral change
(both roots chunked incl. lod_ai/tests/commands/, gate 1-10/11-20,
soak 120 via resume — DELETE the old soak.jsonl first, it resumes
completed files; balance per scenario + --update on flips); work in
~/work clone, python3-heredoc edits with count asserts,
PYTHONHASHSEED=0 for replays; 20-game smoke reads are noise — the
soak --out + Wilson aggregation (see docs/balance_largeN_s53.md) is
the instrument.
