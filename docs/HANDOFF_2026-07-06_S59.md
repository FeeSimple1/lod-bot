# Handoff — July 6 2026, after Session 59

HEAD: see git log (2 commits this session).  All green: 1,412 + 101
tests (2 rewritten to §8.1.2 with citations), gates 1-20, soak 120
DONE clean, balance rebaselined, large-N rerun.
Opener: "Read docs/HANDOFF_2026-07-06_S59.md and continue from Next."

## The session in one paragraph
D1-D3 fixed and the Example-5 golden is GREEN end-to-end (mix, Brant
die, War Path target, fees, CBC/CRC all match the designer's
walk-through).  D1's true shape: the Scout's R/T mix now follows the
§8.1.2 alternation idiom under the §3.4.3 caps (the S58 "no WP" read
was a history-logging artifact).  D2: post-Scout leader follow is
move-aware with the Playbook's tie die (1-3 follows).  D3: War Path
ranks by REMOVABLE pieces (option feasibility), the fort tier is
option-3-feasibility-gated (a latent abort trap), the Village
tiebreak is live, and the executor removes Militia before cube types
(both option blocks).  Test-harness hardening: _ScriptedRng needs
__deepcopy__ + no-dunder-forwarding because the engine sandboxes
each bot turn via deepcopy (a recursing wrapper became a silent
INDIAN PASS).  Large-N: statistically unchanged (B 5/300) —
correctness, not a lever; Scout fires ~once/game.

## Next queue  (UPDATED late-S59: Example 3 landed same session)
1. Playbook goldens Examples 2 and 4 (line ranges in the
   test_playbook_goldens.py header; Example-5/-3 pattern:
   precondition asserts, _ScriptedRng — now with a D3 script arg —
   and state["ineligible_next"] isolation).  Example 3 is DONE and
   was the project's biggest single find: the Muster planner was
   blind to its own plan (see audit_report S59) — British 5->20/300,
   CIs exclude zero for the first time.  Example 2 (Garrison +
   expulsion, 1073-1197) and Example 4 (BS exemplar, 1325-1426)
   remain.  Two-column text interleaves — reconstruct column order
   before transcribing.
1b. NEW OUTLIER for review: Indians 46% in 1775 (largeN_s59b) after
   the Royalist-side fixes; Patriots 10% there.  Audit-first doctrine
   still applies — but all four flowcharts are text-verified now, so
   the next instrument is Example 2/4 plus the §8.2 Random-Spaces-
   table question (our seeded ties vs the printed table — consider
   filing as Q22 if any golden hinges on it).
2. Known gap from D2: the generic follow-after-move used by the
   Indian March/Raid paths still uses the WQ most-WPs redeploy
   heuristic; no golden exercises it yet.  If Example 2-4 don't
   cover leader follows, consider a rules-first fix (same move-aware
   pattern) with unit tests.
3. Remaining Piece 3 card ledger; TRACEABILITY_CH1_7 C-series;
   Pieces 4/6/9.

## Awaiting Eric
Nothing.  Q1-Q21 all resolved.  Standing meta-question (British ~1%
in bot-vs-bot) unchanged — the remaining goldens are the last
fidelity instrument before it becomes a design determination.
